--- #EC2 Remote facts using instance id
- hosts: "{{ var_hosts }}"
  remote_user: "{{ var_remote_user }}"
  become: "{{ var_become }}"
  connection: "{{ var_connection }}"
  gather_facts: "{{ var_gather_facts }}"
  vars_files:
   - files/vars_local.yml
  tasks:
   -  name: Remote facts using instance id
      ec2_remote_facts:
        region: "{{ aws_region }}"
        filters:
         instance-id: "{{ instance_id }}"
      register: facts_find
   -  name: Printing Private IP address
      debug: msg="{{ facts_find.instances[0].private_ip_address }}" 
